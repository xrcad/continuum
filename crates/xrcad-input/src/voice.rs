//! Voice input adapter — audio capture -> clamped LLM -> role events.
//! Stub.

use bevy::prelude::*;

pub struct VoiceInputPlugin;

impl Plugin for VoiceInputPlugin {
    fn build(&self, _app: &mut App) {}
}
